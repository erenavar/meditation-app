import React from "react";
import { StyleSheet, View } from "react-native";

export default function Line() {
  return (
    <View
      style={{
        borderBottomColor: "#B0C9C7",
        borderBottomWidth: StyleSheet.hairlineWidth,
        alignSelf: "center",
        width: "90%",
      }}
    />
  );
}
